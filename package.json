{
  "name": "movie-scraper-cron",
  "version": "1.0.0",
  "description": "Movie scraper with scheduled runs on Vercel",
  "engines": {
    "node": "18.x"
  },
  "dependencies": {
    "mysql2": "^3.6.0",
    "node-fetch": "^2.6.9"
  },
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel deploy --prod"
  },
  "devDependencies": {
    "vercel": "^30.2.3"
  }
}
